#include <stdio.h>
int main(void) {
    
  int x;
  scanf("%d", &x);
  fflush(stdin);
 // printf("x1=%d", x);6
 // x = x*2;
 // printf("x2=%d", x);
  char str[x];
  for (int i=0; i < x; i++) {
     // printf("i=%d ", i);
      scanf(" %c", &str[i]);
  }
int z = 0;  
for (int y = 65; y <= 90 ; y++) {
     for (int i=0; i < x; i++) {
         //printf("y=%c str=%c \n", y, str[i]);
         if ((str[i] == y) || (str[i] == y+32)) {
             z++;
         }
     }
printf("%d ", z);
z=0; 
}


/*
char str[2] = "a";
char y = 97;
int res = (str[0] == y);
printf ("%d", res);
*/
  return 0;
}